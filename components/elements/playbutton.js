// Tu botón existente con el ícono de play
const playButton = document.getElementById('play-button');

playButton.addEventListener('click', () => {
  // Llamar a la función para mostrar el video
  handlePlayButtonClick();
});
